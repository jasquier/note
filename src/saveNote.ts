// Save a note to the database under the given topic
export function saveNote(topic: string, note: string): void {
  console.log(`Your topic is ${topic} and your note is ${note}`);
  console.log("\n    Coming soon: Saving a note!");
}
